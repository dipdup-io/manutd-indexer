# generated by DipDup 8.0.0b5
[tool.poetry]
name = "manutd_indexer"
version = "0.0.1"
description = "A blockchain indexer built with DipDup"
license = "MIT"
authors = [
    "John Doe <john_doe@example.com>",
]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
# dipdup = ">=8.0.0b5,<9"
dipdup = {git = "https://git@github.com/dipdup-io/dipdup.git", rev = "aux/8.0.0"}

[tool.poetry.group.dev.dependencies]
black = "*"
ruff = "*"
mypy = "*"

[tool.black]
line-length = 140
target-version = ['py312']
skip-string-normalization = true

[tool.ruff]
line-length = 140
target-version = 'py312'

[tool.ruff.lint]
extend-select = ["B", "C4", "FA", "G", "I", "PTH", "Q", "RET", "RUF", "TCH", "UP"]
flake8-quotes = { inline-quotes = "single", multiline-quotes = "double" }
isort = { force-single-line = true}

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]
strict = false
exclude = "manutd_indexer"

[tool.mypy-ruamel.yaml]
ignore_missing_imports = true


[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"